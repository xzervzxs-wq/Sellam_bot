# 🧪 دليل اختبار نظام الدخول

## ❌ المشاكل الشائعة والحلول

### **المشكلة 1: يظهر "❌ الكود غير صحيح" مع أن الكود صحيح**

**السبب:** فشل تحميل البيانات من Google Sheet (مشكلة CORS)

**الحل:**
1. افتح متصفح Chrome
2. اضغط `F12` (أو Ctrl+Shift+I)
3. اذهب إلى `Console` (الجهة اليسرى)
4. سترى رسائل مثل:
   - ✅ "تم تحميل البيانات: { 1001: {...} }"
   - ❌ "خطأ في تحميل البيانات"

**إذا رأيت الخطأ:**
- تفعيل VPN أو proxy
- أو استخدم متصفح مختلف

---

### **المشكلة 2: شاشة الدخول لا تظهر**

**السبب:** مشكلة في تحميل الملف نفسه

**الحل:**
1. تأكد من أن الملف HTML موجود
2. تأكد من أن الرابط صحيح في المتصفح
3. حاول Refresh الصفحة (F5)

---

### **المشكلة 3: الكود صحيح لكن يقول "انتهى الاشتراك"**

**السبب:** تاريخ الانتهاء أقل من تاريخ اليوم

**الحل:**
1. تأكد من صيغة التاريخ: `DD-MM-YYYY` (مثل `26-01-2026`)
2. التاريخ يجب أن يكون ≥ تاريخ اليوم
3. اليوم هو: `18-01-2026`
4. لذا يجب أن يكون التاريخ مثلاً: `20-01-2026` أو أكثر

**مثال:**
```
❌ غلط: 2026-01-26 (صيغة ISO خاطئة للـ Sheet)
❌ غلط: 2025-12-31 (ماضي)
✅ صحيح: 26-01-2026 (صيغة صحيحة ومستقبلية)
```

---

## 🔧 خطوات التصحيح الكاملة

### **خطوة 1: فتح Developer Console**
```
Windows/Linux: Ctrl + Shift + I
Mac: Cmd + Option + I
```

### **خطوة 2: اختبار البيانات**

في Console اكتب:
```javascript
// عرض البيانات المحملة
console.log(subscriptionData);

// عرض جميع الأكواس المتاحة
console.log(Object.keys(subscriptionData));

// اختبار كود معين
console.log(subscriptionData['1001']);
```

### **خطوة 3: اختبار التاريخ**

```javascript
// اليوم
const today = new Date();
console.log("اليوم:", today);

// اختبار تاريخ
const expiryDate = new Date('2026-01-26');
console.log("تاريخ الانتهاء:", expiryDate);

// هل ساري؟
console.log("ساري؟", expiryDate >= today);
```

---

## 📊 مثال عملي: ما يجب أن يكون في Google Sheet

| Code | Name | ExpiryDate |
|------|------|-----------|
| 1001 | ياسر | 26-01-2026 |

**الاختبار:**
1. أدخل الكود: `1001`
2. اضغط دخول
3. يجب أن يدخل التطبيق

---

## 🔍 معلومات التصحيح المدمجة

في شاشة الدخول:
1. انقر على **"🔧 معلومات تصحيح"** (أسفل يسار)
2. سترى جميع الأكواس المتاحة
3. تأكد من أن الكود الخاص بك موجود

---

## ✅ علامات النجاح

✅ في Console ترى: "✅ تم تحميل البيانات"
✅ الأكواس تظهر في معلومات التصحيح
✅ بعد دخول الكود الصحيح، يختفي الكود الأول
✅ عنوان الصفحة يتغير إلى "أستوديو [الاسم]"

---

## 📞 أكثر شيء يفعل النظام يشتغل:

### إذا ما اشتغل مع Netlify/Hosting:

جرب رابط مباشر لـ Google Sheet:
```
https://docs.google.com/spreadsheets/d/e/2PACX-1vTiWBnSkPTGN8S3-QjqwSjpJicszKs2ZuwI40iPph2EWQhgz9F10c7LIjMwa_cdPJr40OoDmqZDbP5F/pub?output=csv
```

افتحه في المتصفح مباشرة لتتأكد أنه يعمل.

---

**النظام جاهز! 🚀**
