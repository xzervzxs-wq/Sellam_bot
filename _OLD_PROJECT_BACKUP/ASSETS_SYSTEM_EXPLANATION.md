# شرح نظام مكتبة العناصر 📚

## نظام العمل الحالي

### 1. **كيف يعمل الآن:**

```javascript
// الخطوة 1: تحميل الملف كاملاً من GitHub
fetch('https://raw.githubusercontent.com/xzervzxs-wq/Sellam_bot/main/Official.json')
    .then(response => response.json())
    .then(data => {
        officialAssetsLibrary = data; // تخزين البيانات في الذاكرة
        // الآن يمكن استخدامها دون إعادة تحميل
    });

// الخطوة 2: عند اختيار تصنيف، يتم عرض العناصر
const freeCount = category.freeCount || 4; // الافتراضي 4 عناصر للمجاني
const isLocked = index >= freeCount && userTier === 'free';
// إذا كانت الفهرسة (index) أكبر من أو تساوي freeCount → مقفولة
```

### 2. **بنية الملف JSON (Official.json):**

```json
[
  {
    "name": "اسم التصنيف",
    "freeCount": 4,  // عدد العناصر المفتوحة للمجانيين
    "items": [
      {
        "src": "https://example.com/image.png",
        "colorable": true  // يمكن تغيير لونه
      },
      { ... }
    ]
  }
]
```

---

## الخيارات المتاحة لك:

### ✅ **الخيار 1: الطريقة الحالية (الأفضل للملفات الكبيرة)**

**المزايا:**
- ✅ تحميل مرة واحدة ثم استخدام من الذاكرة
- ✅ سريع جداً بعد التحميل الأول
- ✅ لا حاجة لاتصال إنترنت بعد التحميل

**العملية:**
1. حدّث الملف `Official.json` في GitHub
2. أضف عناصر جديدة لأي تصنيف
3. عند الفتح التالي، سيحمل الملف الجديد تلقائياً (الـ `?t=' + Date.now()` تضمن عدم التخزين المؤقت)

**كود يمكن تحسينه للتحديث القسري:**
```javascript
// أضف زر "تحديث المكتبة" في الواجهة
function refreshAssetsLibrary() {
    officialAssetsLibrary = []; // مسح الذاكرة
    loadAssetsLibraryFromGitHub(); // إعادة التحميل
}
```

---

### 📦 **الخيار 2: تقسيم الملفات (للملفات الكبيرة جداً)**

إنشاء ملف فهرس صغير + ملفات منفصلة لكل تصنيف:

```
Official.json (فهرس صغير):
[
  {
    "name": "الأيقونات",
    "freeCount": 4,
    "file": "categories/icons.json"
  },
  {
    "name": "الأشكال",
    "freeCount": 4,
    "file": "categories/shapes.json"
  }
]

// عند الاختيار:
fetch(`https://raw.githubusercontent.com/.../categories/icons.json`)
    .then(data => loadCategory(data));
```

**المزايا:** تحميل أسرع للفهرس، وتحميل التصنيف فقط عند الحاجة

---

### ⚡ **الخيار 3: Pagination (تحميل تدريجي)**

تحميل 4 عناصر فقط في البداية، ثم تحميل المزيد عند التمرير:

```javascript
let currentPage = 0;
const itemsPerPage = 4;

function loadMoreItems() {
    const startIdx = currentPage * itemsPerPage;
    const endIdx = startIdx + itemsPerPage;
    const moreItems = category.items.slice(startIdx, endIdx);
    // عرض العناصر + زر "تحميل المزيد"
    currentPage++;
}
```

---

## التوصية 🎯

**استخدم الخيار 1 (الطريقة الحالية)** لأن:
1. بسيطة وفعالة
2. لا تحتاج تغييرات كبيرة
3. الملف يتحدث تلقائياً عند إعادة التحميل

**العملية:**
```bash
# كل ما تحتاج:
1. حدّث الملف Official.json محلياً
2. ارفعه لـ GitHub
3. عند فتح المكتبة مرة التالية، سيحمل الملف الجديد تلقائياً
4. بدون حذف أو تحديثات إضافية!
```

**كود تحديث الذاكرة المخزنة (optional):**
```javascript
// أضف هذا إذا أردت تحديث يدوي للمكتبة بدون إعادة تحميل الصفحة
function manuallyRefreshAssets() {
    officialAssetsLibrary = []; // حذف من الذاكرة
    const select = document.getElementById('assets-category-select');
    select.innerHTML = '<option value="">📂 جاري التحديث...</option>';
    loadAssetsLibraryFromGitHub(); // إعادة التحميل من GitHub
}
```

---

## الخلاصة ✨

- **لا تحذف الملف القديم**
- **فقط حدّث الملف وأعد رفعه**
- **النظام سيحمل الجديد تلقائياً**
- **كل عنصر تضيفه: سيكون مقفول أم مفتوح حسب `freeCount`**
