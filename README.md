# Sellam_bot

🤖 بوت سلام الذكي - بوت تليجرام متعدد الوظائف مع دعم الدردشة بالذكاء الاصطناعي ومراقبة الأسهم

## ✨ المميزات

### 💬 الدردشة الذكية
- دردشة مع نماذج GPT المتقدمة
- دعم النموذج المميز GPT-4 والنموذج القياسي GPT-3.5
- تبديل تلقائي من المميز إلى القياسي عند تجاوز الحد
- واجهة عربية كاملة

### 📊 مراقبة الأسهم
- مراقبة حية للأسهم كل 5 دقائق
- اكتشاف فرص التداول (استراتيجية السلم الصاعد)
- إشعارات فورية على تليجرام

## 🚀 التثبيت والإعداد

### 1. تثبيت المتطلبات

```bash
pip install requests pandas yfinance python-dotenv
```

### 2. إعداد المفاتيح

أنشئ ملف `.env` في المجلد الرئيسي:

```bash
# OpenAI API Key - مطلوب للدردشة
OPENAI_API_KEY=sk-your-key-here

# EODHD API Key - اختياري لبيانات الأسهم
EODHD_API_KEY=your-key-here
```

### 3. الحصول على مفتاح OpenAI API

1. اذهب إلى [OpenAI Platform](https://platform.openai.com/api-keys)
2. سجل دخول أو أنشئ حساب جديد
3. انقر على "Create new secret key"
4. انسخ المفتاح وضعه في ملف `.env`

### 4. تشغيل البوت

```bash
python3 main
```

## 💡 كيفية الاستخدام

### أوامر التليجرام

- `/start` - بدء المحادثة مع البوت
- `/help` - عرض المساعدة والأوامر المتاحة
- `/premium` - التبديل إلى نموذج GPT-4 (مميز)
- `/standard` - التبديل إلى نموذج GPT-3.5 (قياسي)

### استخدام الدردشة

فقط أرسل رسالة نصية عادية للبوت وسيرد عليك باستخدام الذكاء الاصطناعي!

مثال:
```
أنت: ما هي أفضل طريقة لتعلم البرمجة؟
البوت: [رد مفصل من GPT]
```

## 🌟 حول النماذج المميزة

### GPT-4 (المميز) 🌟
- نموذج متقدم بقدرات فهم وتحليل فائقة
- محدود بعدد معين من الطلبات حسب خطة OpenAI
- يتم استخدامه افتراضياً مع التراجع التلقائي

### GPT-3.5 (القياسي) ⚡
- نموذج سريع وموثوق
- غير محدود ضمن حدود خطتك
- يتم التبديل إليه تلقائياً عند تجاوز حد GPT-4

## ❓ حل مشكلة "تجاوز حد الطلبات المميزة"

إذا واجهت الرسالة:
```
You have exceeded your premium request allowance...
```

**الحلول:**

1. **تلقائي**: البوت يبدل تلقائياً إلى GPT-3.5 (لا حاجة لأي إجراء)

2. **زيادة الحد**: 
   - اذهب إلى [OpenAI Usage](https://platform.openai.com/usage)
   - اختر خطة أعلى أو أضف رصيد
   - ستتمكن من استخدام GPT-4 بشكل أكبر

3. **استخدام النموذج القياسي فقط**:
   - عدل الكود واستخدم `use_premium=False` افتراضياً
   - أو استخدم أمر `/standard` في التليجرام

## 📝 ملاحظات

- البوت يعمل بشكل مستمر ويعالج الرسائل والأسهم في نفس الوقت
- يتم حفظ آخر رسالة معالجة لتجنب التكرار
- التبديل التلقائي للنماذج يضمن عدم انقطاع الخدمة

## 🔧 التخصيص

### تغيير قائمة الأسهم المراقبة

عدل المتغير `watch_list` في الدالة `run_stock_monitor()`:

```python
watch_list = ["TSLA", "AAPL", "MSFT"]  # أضف أو أزل الأسهم
```

### تغيير فترة المراقبة

عدل المتغير `stock_check_interval` في الدالة `main()`:

```python
stock_check_interval = 600  # 10 دقائق بدلاً من 5
```

## 📄 الترخيص

مشروع مفتوح المصدر - استخدمه بحرية!
