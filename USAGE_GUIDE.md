# دليل الاستخدام السريع - Sellam Bot Quick Start Guide

## 🚀 البدء السريع

### 1. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 2. إعداد البيئة

انسخ ملف `.env.example` إلى `.env`:

```bash
cp .env.example .env
```

ثم عدل ملف `.env` وأضف مفتاح OpenAI API الخاص بك:

```
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxx
```

### 3. تشغيل البوت

```bash
python3 main
```

## 💬 استخدام الدردشة

### الأوامر الأساسية

- **`/start`** - بدء المحادثة مع البوت
- **`/help`** - عرض قائمة الأوامر والمساعدة
- **`/premium`** - التبديل إلى GPT-4 (نموذج مميز)
- **`/standard`** - التبديل إلى GPT-3.5 (نموذج قياسي)

### أمثلة على الاستخدام

```
أنت: /start
البوت: 👋 مرحباً! أنا بوت سلام للدردشة الذكية...

أنت: ما هي أفضل استراتيجيات الاستثمار؟
البوت: [إجابة مفصلة من GPT]
النموذج المستخدم: GPT-4 🌟

أنت: اشرح لي البرمجة الكائنية
البوت: [شرح تفصيلي]
النموذج المستخدم: GPT-3.5 ⚡
```

## 🔧 حل المشاكل الشائعة

### مشكلة: "You have exceeded your premium request allowance"

**الحل التلقائي:** 
البوت يتعامل مع هذا تلقائياً! عند تجاوز حد GPT-4، سينتقل تلقائياً إلى GPT-3.5 دون انقطاع الخدمة.

**الحلول اليدوية:**

1. **الانتظار:** حد الطلبات يتجدد مع الوقت حسب خطتك
2. **الترقية:** ادخل إلى [OpenAI Usage](https://platform.openai.com/usage) واختر خطة أعلى
3. **استخدام GPT-3.5:** أرسل `/standard` لاستخدام النموذج القياسي دائماً

### مشكلة: "لم يتم تكوين مفتاح OpenAI API"

**الحل:**
1. تأكد من وجود ملف `.env` في المجلد الرئيسي
2. تأكد من أن الملف يحتوي على: `OPENAI_API_KEY=sk-your-key`
3. احصل على المفتاح من [OpenAI API Keys](https://platform.openai.com/api-keys)

### مشكلة: البوت لا يرد على الرسائل

**الحل:**
1. تأكد من أن البوت يعمل (`python3 main`)
2. تحقق من معرف الدردشة في ملف `main`
3. تأكد من اتصالك بالإنترنت

## 📊 النماذج المتاحة

### GPT-4 (المميز) 🌟
- **المميزات:** فهم عميق، إجابات دقيقة، قدرات متقدمة
- **السلبيات:** محدود بعدد طلبات حسب الخطة
- **الاستخدام المثالي:** أسئلة معقدة، تحليلات عميقة

### GPT-3.5 Turbo (القياسي) ⚡
- **المميزات:** سريع، موثوق، غير محدود ضمن خطتك
- **السلبيات:** أقل تقدماً من GPT-4
- **الاستخدام المثالي:** أسئلة عامة، محادثات سريعة

## 🔐 الأمان

- **لا تشارك مفتاح API الخاص بك** مع أي شخص
- **لا ترفع ملف `.env`** إلى GitHub أو أي مكان عام
- **احفظ نسخة احتياطية** من مفتاح API في مكان آمن

## 📈 مراقبة الاستخدام

تتبع استخدامك على:
- [OpenAI Usage Dashboard](https://platform.openai.com/usage)

يمكنك رؤية:
- عدد الطلبات المتبقية
- التكلفة التقديرية
- سجل الاستخدام

## 🆘 الدعم

إذا واجهت مشكلة:
1. راجع قسم حل المشاكل أعلاه
2. تحقق من سجلات البوت في Terminal
3. تأكد من تحديث جميع المكتبات: `pip install -r requirements.txt --upgrade`

---

**ملاحظة:** هذا البوت يعمل بشكل مستمر ويعالج الرسائل في الوقت الفعلي. يمكنك إيقافه بالضغط على `Ctrl+C` في Terminal.
